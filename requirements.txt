openai>=1.40
anthropic>=0.34
python-dotenv>=1.0
pydantic>=2.7
streamlit>=1.36
requests>=2.31
sqlalchemy>=2.0
# Postgres driver — only needed when SIGNALS_DB_URL points at Postgres (e.g. the hosted demo).
# Harmless locally, where the default is file-based SQLite.
psycopg2-binary>=2.9
